{
  "name": "ethernal-strike-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev -- --hostname 0.0.0.0 --port 3000",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start"
  }
}
